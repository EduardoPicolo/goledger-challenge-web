import styled from 'styled-components';

const ErrorContainer = styled.span`
  color: red;
  position: absolute;
  align-self: center;
  bottom: -1px;
  font-weight: 600;
`;

export default ErrorContainer;
